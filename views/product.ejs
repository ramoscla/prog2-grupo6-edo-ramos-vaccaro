<!DOCTYPE html>
<html lang="es">

<!-- Include Head -->
<%- include("./partials/head.ejs") %>
<!-- Include Head -->

<body>
	<!-- Header -->
	<%- include("./partials/header.ejs") %>
	<!-- /Header -->
	
	<!-- product detail -->
	<div class="container products-wrapper">
		<div class="d-flex align-items-center">
			<h2 class="products-title"><%= productos[0].nombre %></h2> 
			<p class="product-detail-description"> <a href="/product/add">Editar</a> </p>
		</div>

		<div class="product-detail">
			<div class="row">

				<article class="col-12 col-lg-8 product-detail-info">
					<img src="/images/products/<%= productos[0].foto %>" alt="foto del producto" class="foto-perfil">
					<p class="product-detail-description">
						<%= productos[0].descripcion %>
						</p>
				</article>

				<div class="col-12 col-lg-4">
					<section class="comments">
						<% for (let i = 0; i < productos[0].comentarios.length; i++) { %>
							<article>
								<img src= "/images/users/<%=  productos[0].comentarios[i].fotoPerfil%>"> 
								<div> 
									<p> <a href="/users/profile"> <%=productos[0].comentarios[i].nombreUsuario%> : </a> <%= productos[0].comentarios[i].texto%> </p>
								</div>
							</article>
						<% } %>
					</section>

					<form class="formularios" action="/product" method="get">
						<input class="form-input" type="text" placeholder="Agregar comentario">
						<button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button>
					</form>
					
				</div>

			</div>
		</div>
	<!-- /product detail -->

	<!-- footer -->
	<%- include('./partials/footer.ejs') %>
	<!-- /footer -->

</body>	
</html>